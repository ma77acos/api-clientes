# ============================================================
#  Overrides locales (perfil: local) — Windows / máquina de Marcos
#  Uso: gradle bootRun --args='--spring.profiles.active=dev,local'
# ============================================================

# MySQL Windows
spring.datasource.url=jdbc:mysql://localhost:3306/reservas_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=America/Argentina/Buenos_Aires&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Ma77acos

# Mail (misma cuenta que el resto de apps; override con SMTP_PASSWORD si hace falta)
spring.mail.username=fastbuild.dev@gmail.com
spring.mail.password=${SMTP_PASSWORD:}

cors.allowed.origins=http://localhost:3000,http://localhost:3001
app.frontend-url=http://localhost:3000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

logging.level.com.reservas=DEBUG
logging.level.org.springframework.security=DEBUG
